
def (foldl f acc [] ...) acc
def (foldl f acc [xs lsts ...] ...)
  (foldl f (f xs ... acc) lsts ...)

def (map f [] ...) []
def (map f [xs lsts ...] ...)
  [(f xs ...) (map f lsts ...) ...]

def (add x y)
  x + y

def (add1 x)
  x + 1

def (main)
{
  (debug_print (write (foldl add 0 [1 2 3 4 5])))
  (debug_print (write (map add1 [1 2 3 4])))
}