def (main)
    let a (new `foo 10 (lambda (x)
        (new_set x 2 1050);
        (new_set x 0 (new_get x 2));
        (new_set x 5 (new_size x));
        (new_set x 8 (new_render x))
    ))

    (debug_print (write a))
