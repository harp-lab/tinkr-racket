def (print_list x)
    if (= (slice_len x) 0)
        `print <- "\n"

        `print <- (prim_int_to_string (first x));
        (print_list (rest x))

def (main)
    let a [1 2 3]
    let b (print_list a)
    (debug_print (write b))
